# tests/plugin/

PLANNED (docs/ROADMAP.md Phase 20). Reserved for `pluginval` and host-matrix
automation once `pw8_plugin` builds against real JUCE (currently an untested
scaffold, see `plugin/CMakeLists.txt`). Not part of `pw8_tests` / `ctest` in this pass.
