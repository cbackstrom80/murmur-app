# tools/algorithm_inspector/

Superseded in scope by `tools/graph_inspector/` (built as `pw8-graph`), which
already covers algorithm graph inspection (`pw8-graph inspect <preset>`). This
directory is reserved in case algorithm-*library*-wide inspection (validating/listing
every template under `content/algorithms/` at once) becomes a distinct tool.
