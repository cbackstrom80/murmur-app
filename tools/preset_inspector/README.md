# tools/preset_inspector/

PLANNED. Reserved for a tool that prints a patch's full parameter tree, metadata,
and lock flags in a readable form (complementary to `murmur-graph`, which focuses on
the algorithm graph specifically). In this pass, `murmur-graph inspect` already prints
patch name/category plus per-layer graph structure.
