# plugin/src/parameters/

PLANNED (docs/ROADMAP.md Phase 16). Will hold the `juce::AudioProcessorValueTreeState`
wiring for the macro/performance parameters described in
`plugin/src/state/PluginState.h`. Not yet implemented -- the plugin target itself is
an untested scaffold (see `plugin/CMakeLists.txt`).
